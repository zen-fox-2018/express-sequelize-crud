<!DOCTYPE html>
<html lang="en">
<head>
    <title>Subjects List</title>
    <style>
        table {
            border: 1px solid rgb(0, 0, 0);
            background: white;
        }
        th, td {
            padding: 15px;
            text-align: left;
            border: 1px solid rgb(0, 0, 0);
        }
        /* body { */
            /* background-image: url("https://images.pexels.com/photos/448877/pexels-photo-448877.jpeg?auto=compress&cs=tinysrgb&dpr=1&w=500"); */
            /* background-size: cover; */
        /* } */
        th {
            background: Yellow;
        }
        #Judul {
            color: greenyellow;
        }
    </style>
</head>
<body>
    <h1> <%= title %> </h1>
    <h5><a href="/subjects/add">add subject</a></h5>
    <table>
        <tr>
            <th>id</th> 
            <th>subject_name</th>
        </tr>
        <% allData.forEach(e => { %>
        <tr>    
            <td><%= e.id %></td>
            <td><%= e.subject_name %></td>
        </tr>
        <% }) %>
    </table>
</body>
</html>